import React from "react";

function KnowMore() {
  return (
    <div className="UI-container">
      <h1>How do we know?</h1>
      <p>It takes about (X.X)g of CO2 to produce a cookie. Each google search emits about (XX)g of CO2.</p>
      <p>In the algorithm used, X is cookies and y is the searches:</p>

      <blockquote>algorithm here</blockquote>
      <button>Home</button>
    </div>
  );
}

export default KnowMore;
